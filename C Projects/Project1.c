#include <stdio.h>
int main (void)
{
	float mass;
	float gravity = 9.8;
	float s_value;
	float velocity = (mass*gravity)/(s_value*fluid_viscosity);

	printf("Please enter the mass of the object, or 'm': ");
	scanf("%f/n",&mass);
	printf("Please enter Stoke's value, or 

}

